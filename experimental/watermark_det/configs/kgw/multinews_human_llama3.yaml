# General configuration
general:
  run_name: "detection_human_multinews"

# Model configuration
model:
  name: "meta-llama/Meta-Llama-3-8B-Instruct"
  transformers_config_kawrgs: {}

# Watermark configuration
watermark:
  algorithm_name: "KGW"
  generation_kwargs: {}

# File configuration
file:
  root_dir: "PUPPET/experimental"
  generation:
    path: "submodules/WaterBench/data/WaterBench/4-1_multi_news.jsonl"
    field: "outputs"
    extract:
      method: flatten
      kwargs: {}
    preprocess: []
  output:
    path: "watermark_det/results"
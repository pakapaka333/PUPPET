# General configuration
general:
  run_name: "detection_watermark_multi_news_llama3_synthid_paraphrased"

# Model configuration
model:
  name: "meta-llama/Meta-Llama-3-8B-Instruct"
  transformers_config_kawrgs: {}

# Watermark configuration
watermark:
  algorithm_name: "SynthID"
  generation_kwargs: {}

# File configuration
file:
  root_dir: "PUPPET/experimental"
  generation:
    path: "paraphrase/results/paraphrase_watermark_multinews_llama3_synthid_2026_0120_173240.jsonl"
    field: "paraphrase_outputs"
    extract:
      method: "flatten"
      kwargs: {}
    preprocess: []
  output:
    path: "watermark_det/results"

# Device configuration
device:
  server: "hestia"
  type: "cuda"
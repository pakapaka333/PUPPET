# General configuration
general:
  run_name: "paraphrase_watermark_ielts_llama3_synthid"

# Paraphrase configuration
paraphrase:
  model_kwargs:
    dtype: "float16"
  generation_kwargs:
    lex_diversity: 60
    order_diversity: 60
    do_sample: true
    top_p: 0.75
    top_k: null
    max_length: 512
  preprocess:
    - name: "extract_answer_with_regex"
      kwargs:
        full_output_field: "watermarked_model_output"
        regex_pattern: 'assistant\s*\n\s*\n(.*)$'
  max_samples: null

# File configuration
file:
  root_dir: "PUPPET/experimental"
  generation:
    path: "watermark_gen/data/baselines/synthid/generation_watermark_ielts_llama3_2026_0120_132436_synthid.jsonl"
    field: "watermarked_model_output"
  output:
    path: "paraphrase/results"
# General configuration
general:
  run_name: "evaluation_watermark_multi_news_qwen3_kgw"

# Evaluation configuration
evaluation:
  metrics:
    rouge_score_waterbench:
      reference:
        path: "watermark_gen/data/baselines/kgw/generation_watermark_multi_news_qwen3_kgw_2026_0127_182054.jsonl"
        field: "outputs"
  preprocess:
    - name: "extract_answer_with_regex"
      kwargs:
        full_output_field: "watermarked_model_output"
        regex_pattern: 'assistant\s*\n<think>\s*\n\s*</think>\s*\n\s*(.*)$'
  max_samples: null

# File configuration
file:
  root_dir: "PUPPET/experimental"
  generation:
    path: "watermark_gen/data/baselines/kgw/generation_watermark_multi_news_qwen3_kgw_2026_0127_182054.jsonl"
    field: "watermarked_model_output"
  output:
    path: "evaluation/results"
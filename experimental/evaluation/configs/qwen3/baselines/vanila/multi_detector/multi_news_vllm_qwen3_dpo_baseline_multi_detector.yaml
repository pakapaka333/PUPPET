# General configuration
general:
  run_name: "evaluation_vllm_multi_news_qwen3_dpo_baseline_multi_detector"

# Evaluation configuration
evaluation:
  metrics:
    rouge_score_waterbench:
      reference:
        path: "vllm_gen/data/baselines/vanila/generation_vllm_multi_news_qwen3_dpo_baseline_2025_1208_154737.jsonl"
        field: "outputs"
    detect_fakespotai:
      kwargs:
        detector_kwargs:
          device_map: "auto"
    detect_mage:
      kwargs:
        detector_kwargs:
          device_map: "auto"
  preprocess: {}
  max_samples: null

# File configuration
file:
  root_dir: "PUPPET/experimental"
  generation:
    path: "vllm_gen/data/baselines/vanila/generation_vllm_multi_news_qwen3_dpo_baseline_2025_1208_154737.jsonl"
    field: "vllm_model_output"
  output:
    path: "evaluation/results"
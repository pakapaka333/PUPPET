# General configuration
general:
  run_name: "evaluation_vllm_longform_qa_qwen3_5000"

# Evaluation configuration
evaluation:
  metrics:
    rouge_score_waterbench:
      reference:
        path: "vllm_gen/data/for_dpo_data/.jsonl"
        field: "outputs"
    detect_openai:
      kwargs:
        detector_kwargs:
          device_map: "auto"
  preprocess: {}
  max_samples: null

# File configuration
file:
  root_dir: "PUPPET/experimental"
  generation:
    path: "vllm_gen/data/for_dpo_data/.jsonl"
    field: "vllm_model_output"
  output:
    path: "evaluation/results"
# General configuration
general:
  run_name: "evaluation_longform_qa_human"

# Evaluation configuration
evaluation:
  metrics:
    detect_openai:
      kwargs:
        detector_kwargs:
          device_map: "auto"
  preprocess: {}
  max_samples: null

# File configuration
file:
  root_dir: "PUPPET/experimental"
  generation:
    path: "submodules/WaterBench/data/WaterBench/2-1_longform_qa.jsonl"
    field: "outputs"
  output:
    path: "evaluation/results"
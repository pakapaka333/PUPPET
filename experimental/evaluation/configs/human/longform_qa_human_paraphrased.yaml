# General configuration
general:
  run_name: "evaluation_longform_qa_human_paraphrased"

# Evaluation configuration
evaluation:
  metrics:
    detect_openai:
      kwargs:
        detector_kwargs:
          device_map: "auto"
  preprocess: {}
  max_samples: null

# File configuration
file:
  root_dir: "PUPPET/experimental"
  generation:
    path: "paraphrase/results/baselines/paraphrase_longform_qa_human_2026_0210_210145.jsonl"
    field: "paraphrase_outputs"
  output:
    path: "evaluation/results"
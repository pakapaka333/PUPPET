# General configuration
general:
  run_name: "generation_vllm_longform_qa_qwen3_dpo_baseline"

# Model configuration
model:
  name: "Qwen/Qwen3-8B"
  kwargs:
    dtype: "float16"
    tensor_parallel_size: 1

# Generation parameters
generation:
  max_samples: null
  kwargs:
    n: 3
  chat_template_kwargs:
    enable_thinking: false

# File configuration
file:
  root_dir: "PUPPET/experimental"
  input:
    benchmark: "waterbench"
    dataset: "longform_qa"
  output:
    path: "vllm_gen/data"